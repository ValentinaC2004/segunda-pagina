name: documento yml
  on:
    push:
      branches: 
        - main

      jobs:
        pagina-practica:
          steps:
            - name: pagina2
            uses:

            - name: pagina2
            uses: actions/github-pages-deploy-action@4.1.4
            with:
              branch: main
              folder: .
            

